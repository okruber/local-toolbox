// Cube configuration for local data stack
// Connects to DuckDB warehouse and exposes dbt marts
// DB connection is configured via CUBEJS_DB_* environment variables in docker-compose.yaml

module.exports = {};
